[env]
MIX_ENV = "prod"

[tasks]
fetch = "git pull origin release"
release = [
    "mix release ackurat --overwrite",
    "sudo mv _build /opt/ackurat/app"
    ]
replace = "sudo systemctl restart ackurat"

[tools]
elixir = "1.19.5-otp-28"
erlang = "28.3.1"
nodejs = "24.7.0"
